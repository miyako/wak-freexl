(function(w, $, a, m){		function filterEvent(e){		e.stopPropagation();		e.preventDefault();			return false;	}	    	$('body').on('dragover', filterEvent).css({		'-webkit-user-select':'none',		'-moz-user-select':'none',		'-ms-user-select':'none',		'-o-user-select':'none',		'user-select':'none'    });	//jQuery references to page elements		var freexlResult$ = $('#freexl-result');		function afterFileUpload(e){		freexlResult$.text(this.responseText);		a.success('Done!');	}		function uploadFile(url, fileData, cb){		var xhr = new XMLHttpRequest();				xhr.onreadystatechange = function(){			var state = this.readyState;			switch (state){				case 4:					var status = this.status;					switch(status){						case 200:								cb.call(this);							break;						case 404:							a.error('Request handler not found!');							break;							case 401:							a.error('Request handler not authorized!');							break;						default:							a.error('Error!');							break;							}						break;				case 3:						break;				case 2:						break;				case 1:						break;				case 0:						break;				}		}		xhr.open('POST', url);		xhr.setRequestHeader('Content-Type', 'application/vnd.ms-excel'); 		xhr.send(new Uint8Array(fileData));	}			$('body')[0].addEventListener('drop', function(e){		e.stopPropagation();		e.preventDefault();						if(window.FileReader){			var files = e.dataTransfer.files;			function readFile(files, i){				var file = files[i];				var reader = new FileReader();				reader.onload = function(e){					var fileData = e.target.result;					var file = this.file;					if((/.+\.xls/).test(file.name)){						a.log('File: ' + file.name);						uploadFile('xl/tosql', fileData, afterFileUpload);					}					if (i < files.length - 1) {			        readFile(files, i + 1);				    }										}//onload								reader.file = file;				reader.readAsArrayBuffer(file);			}			readFile(files, 0);		}//window.FileReader				}, false);													a.set({'delay':1500});    m.lang(navigator.language);	    		w.onAfterInit = function(){				//just cosmetic, no impact on performance		window.history.replaceState('Object', 'Title', '/');	};			})(WAF, jQuery, alertify, moment);